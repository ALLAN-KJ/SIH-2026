config setup
    charondebug="ike 1, knl 1, cfg 0"
    uniqueids=no

conn %default
    ikelifetime=60m
    keylife=20m
    rekeymargin=3m
    keyingtries=1
    authby=secret
    keyexchange=ikev2

conn test-conn
    left=%any
    right=%any
    ike=aes256cbc-sha256-modp256!
    esp=aes256cbc-sha256-modp256!
    type=transport
    auto=add